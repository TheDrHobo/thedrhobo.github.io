source "https://rubygems.org"

# Use the github-pages meta-gem and pin it to the version GitHub is attempting to use.
# This ensures a clean, compatible set of dependencies.
gem "github-pages", "~> 232"

# Note: You can remove the 'group: :jekyll_plugins' if it was added previously, as it's often not needed.
